# =============================================================================
# Streamlit Deployment Requirements (requirements.txt)
# This file pins library versions to match the Jupyter training environment.
# =============================================================================

# --- Core Libraries ---
streamlit
# Pinned versions from your Jupyter environment:
pandas==2.3.3
numpy
joblib
openpyxl  # Required for reading the .xlsx data file bounds
matplotlib
seaborn

# --- ML Libraries (CRITICAL: Pinned to exact training versions) ---
# This guarantees that the server can load the .joblib models saved 
# using these specific versions.
scikit-learn==1.7.2
xgboost==3.1.2